<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
  <a class="navbar-brand text-warning" href="/">beginmate</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end"  id="navbarNavAltMarkup">
    <div class="navbar-nav align-items-center">

      <a class="nav-item mr-2 nav-link" href="#">메이트 찾기</a>
      <a class="nav-item mr-2 nav-link" href="#">메이트 모집</a>
      <% if user_signed_in? %>

        <div class="dropdown dropleft">
          <% if current_user.profile.attached? %>
            <img src="<%= url_for(current_user.profile) %>" alt="" class="logo_img rounded-circle dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
          <% else %>
            <img src="https://picsum.photos/200" alt="" class="logo_img rounded-circle dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
          <% end %>

          <div class="dropdown-menu">
            <a class="dropdown-item" href="/teams/new">새 팀 생성</a>
            <% current_user.teams.each do |team| %>
              <a class="dropdown-item" href="/teams/<%= team.id %>"><%= team.name %> 팀</a>
            <% end %>
            <a class="dropdown-item" href="/users/sign_out" data-method="delete">로그아웃</a>
          </div>
        </div>
      <% else %>
        <a class="nav-item mr-2 nav-link" href="/users/sign_up">로그인/회원가입</a>
      <% end %>
    </div>
  </div>
</nav>
